
type point = { x: number, y: number };
type line  = { slope: number, intercept: number };
type angle = number;

type luv   = [ number, number, number ];
type hsl   = [ number, number, number ];
type xyz   = [ number, number, number ];





export { point, line, angle, luv, hsl, xyz };
